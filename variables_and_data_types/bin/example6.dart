void main(List<String> args) {
  const int yourAge = 27;
  const myAge = 25;
  print(yourAge);
  print(myAge);

  /// This is explains that there is not need to add the actual type alongside final,const or var keyword
  /// since dart has type inference feature;
}