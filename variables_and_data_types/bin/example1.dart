void main(List<String> args) {
    const foo = 'Hello';
    print(foo);

    //With const,  there are two kinds of immutability;
    /// 1. We cannot reassign the variable to an netirely new value,
    /// 2. The internal content also cannot be changed.
}