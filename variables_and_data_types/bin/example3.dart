void main(List<String> args) {
  var address = '243 Main St';
  print(address);

  address = '456 Main St';
  print(address);

  /// With var keyword, we can perfomr all kinds of mutations to the variable in question unlike
  /// const and final keywords
}